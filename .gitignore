__pycache__/
*.py[cod]

# Per developer local config: plugin installs and permission grants.
.claude/settings.local.json

# Session handoffs are private working notes, not part of the plugin.
# The folder itself stays tracked through .gitkeep, so a fresh clone still
# gets a working relevio setup.
docs/handoff/*.md
